<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#FFFF66</item>
        <item name="colorPrimaryDark">#FFFF66</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#363636</item>
        <item name="colorPrimaryDark">#FFCC00</item>
        <item name="colorAccent">#FFCC00</item>
    </style>

    <style name="style_RadioButton">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">50dp</item>
        <item name="android:button">@null</item>
        <item name="android:background">@null</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textColor">@drawable/rb_text_color_selector</item>
        <item name="android:textSize">10dp</item>
    </style>

    <style name="TextAppearance">
        <item name="android:fontFamily"></item>
    </style>

    <style name="AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ButtonStyle" parent="Base.Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">#FFCC00</item>
    </style>

</resources>
